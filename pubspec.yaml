name: success
description: Conectando emprendedores, empresarios y clientes.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Supabase - Base de datos + Auth + Storage
  supabase_flutter: ^2.3.0

  # State management
  flutter_riverpod: ^2.5.0

  # Navegación
  go_router: ^13.0.0

  # Imágenes
  cached_network_image: ^3.3.1
  image_picker: ^1.0.7

  # Utilidades
  timeago: ^3.6.1
  path: ^1.9.0
  intl: ^0.20.2

  # Iconos
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true